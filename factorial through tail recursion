#include <stdio.h>
int fact(int a,int d){
    if(a==1) {
        return d;
    }
    else{
        d=d*a;
        return(fact(a-1,d));
        
    }
    
}

int main() {
    int n,b,c=1;
    scanf("%d",&n);
    b=fact(n,c);
    printf("%d",b);

    return 0;
}
